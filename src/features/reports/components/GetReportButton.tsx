"use client";

import ButtonWithIcon from "@/features/shared/components/ButtonWithIcon";

const GetReportButton = () => {
  return <ButtonWithIcon label="Get a report" onClick={() => {}} iconName="report" />;
};

export default GetReportButton;
